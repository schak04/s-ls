# DIR*

`DIR` is a type representing a directory stream (provided by the `dirent.h` C header). It is an opaque type. We do not know what it has internally.

#### Ref:
- https://en.wikipedia.org/wiki/Opaque_pointer
- https://blog.mbedded.ninja/programming/design-patterns/opaque-pointers/
